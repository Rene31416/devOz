export async function handler(event:any, context:any) {
    console.log('LOGIN LOGIC')
    //CREATE VALIDATE CREDENTIALS SERVICE, IF OK RETURN TOKEN, IF NOT DENY EVERYTHING
    //IF TOKEN IS EXPIRE, REFRESH IT
    return {
        message:'Method not implementd',
        extra:'this will be a public endpoint'
    }
}